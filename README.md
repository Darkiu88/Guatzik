
  # Expand Prompt for AI

  This is a code bundle for Expand Prompt for AI. The original project is available at https://www.figma.com/design/M1GbrLspHaT1PHmphq4B4m/Expand-Prompt-for-AI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  